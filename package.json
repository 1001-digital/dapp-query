{
  "name": "@1001-digital/dapp-query",
  "version": "0.1.0",
  "description": "Resilient data adapter to power pure dApps.",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "changeset": "changeset",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.30.0",
    "prettier": "^3.5.3"
  },
  "packageManager": "pnpm@10.30.0"
}
